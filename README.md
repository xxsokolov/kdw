# KDW Bot - Управление роутером Keenetic через Telegram

![Status](https://img.shields.io/badge/status-beta-orange.svg)
![Tests](https://img.shields.io/badge/tests-passing-brightgreen.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)

Телеграм-бот для простого и удобного управления службами, прокси и списками обхода блокировок на роутерах Keenetic с установленной системой Entware.

## ⚠️ Статус проекта: Бета

Проект находится в стадии активной разработки. Используйте его с осторожностью. Перед установкой рекомендуется сделать резервную копию настроек роутера.

## 🚀 Основные возможности

*   **Простая установка:** Установка и интерактивная настройка бота одной командой через SSH.
*   **Полный цикл управления:** Установка, переустановка и полное удаление системы.
*   **Управление "по кнопке":** Настройка и управление всей системой обхода блокировок через интуитивно понятное меню в Telegram.
*   **Безопасное удаление:** Полное удаление всех компонентов системы с подтверждением в Telegram.

## ⚙️ Управление установкой

Все действия выполняются через единый скрипт `bootstrap.sh` в консоли роутера.

### Установка

1.  **Установите Entware:** Если у вас не установлен Entware, воспользуйтесь [официальной инструкцией Keenetic](https://help.keenetic.com/hc/ru/articles/360021214160-Установка-системы-пакетов-репозитория-Entware-на-USB-накопитель).
2.  **Подключитесь к роутеру по SSH.**
3.  **Выполните команду:**
    ```sh
    curl -sL https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh | sh
    ```
4.  Следуйте инструкциям в консоли: скрипт сам запросит у вас токен бота и ваш Telegram User ID.

*Примечание: скрипт также поддерживает передачу параметров напрямую, например: `curl ... | sh -s -- --token YOUR_TOKEN --user-id YOUR_ID`.*

### Переустановка

Эта команда полностью удалит старую версию и запустит чистую установку. Выполняется также через SSH.

```sh
curl -sL https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh | sh -s -- --reinstall
```

### Удаление

Эта команда полностью удалит бота и все его компоненты с вашего роутера.

```sh
curl -sL https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh | sh -s -- --uninstall
```

---

## 🐳 Для разработчиков

### Архитектура проекта

Проект использует "пакетную" архитектуру, имитирующую сборку пакетов OPKG.

*   `opkg/`: Директория содержит скрипты, которые выполняются на разных этапах "жизни" пакета (`postinst`, `prerm`, `postrm`).
*   `bootstrap.sh`: Скрипт-загрузчик. Его задача — скачать репозиторий и запустить нужный скрипт из `opkg/` в зависимости от переданных аргументов.

### Тестирование

Для разработки и тестирования используется Docker-окружение на базе **Alpine Linux**.

1.  **Установите Docker Desktop**.
2.  **Создайте и настройте файл `docker/.env`**.
3.  **Запустите тесты:** `pytest -s`
4.  **Принудительная пересборка образа:** `docker-compose -f docker/docker-compose.yml build --no-cache`
