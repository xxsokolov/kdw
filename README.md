# KDW Bot - Управление роутером Keenetic через Telegram

![Status](https://img.shields.io/badge/status-beta-orange.svg)
![Tests](https://img.shields.io/badge/tests-passing-brightgreen.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)

Телеграм-бот для простого и удобного управления службами, прокси и списками обхода блокировок на роутерах Keenetic с установленной системой Entware.

## ⚠️ Статус проекта: Бета

Проект находится в стадии активной разработки. Используйте его с осторожностью. Перед установкой рекомендуется сделать резервную копию настроек роутера.

## 🚀 Основные возможности

*   **Гибкая установка:** Поддержка интерактивной установки через SSH и автоматической через Web CLI.
*   **Управление "по кнопке":** Установка, настройка и управление всей системой обхода блокировок через интуитивно понятное меню в Telegram.
*   **Управление конфигурациями:** Удобное добавление и редактирование ключей для Shadowsocks.
*   **Безопасное удаление:** Полное удаление всех компонентов системы с подтверждением.

## ⚙️ Установка для пользователей

1.  **Установите Entware:** Убедитесь, что на вашем роутере Keenetic установлен компонент системы **"Entware"**. Если нет, воспользуйтесь [официальной инструкцией Keenetic](https://help.keenetic.com/hc/ru/articles/360021214160-Установка-системы-пакетов-репозитория-Entware-на-USB-накопитель).

### Способ 1: Интерактивная установка (рекомендуется)

Этот способ идеально подходит, если вы подключаетесь к роутеру по SSH. Скрипт сам задаст все необходимые вопросы.

1.  Подключитесь к роутеру по SSH.
2.  Выполните команду:
    ```sh
    curl -sL https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh | sh
    ```
3.  Следуйте инструкциям в консоли: введите токен бота и ваш Telegram User ID.

### Способ 2: Автоматическая установка (через Web CLI)

Этот способ позволяет установить бота одной командой, вставив ее в Web CLI роутера.

1.  **Подготовьте команду:** Замените `YOUR_BOT_TOKEN` и `YOUR_USER_ID` на ваши реальные данные в команде ниже.
    ```sh
    curl -sL https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh | sh -s -- --token YOUR_BOT_TOKEN --user-id YOUR_USER_ID
    ```
2.  **Выполните команду:**
    *   Откройте веб-интерфейс роутера (обычно `192.168.1.1`).
    *   Перейдите в раздел **Управление** -> **Общие настройки**.
    *   Найдите поле "Командная строка" или "Web CLI" и вставьте туда вашу подготовленную команду.
    *   Нажмите "Выполнить".

После завершения работы скрипта бот будет установлен, настроен и запущен.

---

## 🐳 Для разработчиков

### Архитектура проекта

Проект использует "пакетную" архитектуру, имитирующую сборку пакетов OPKG.

*   `opkg/`: Директория содержит скрипты, которые выполняются на разных этапах "жизни" пакета.
    *   `postinst`: Скрипт, выполняемый **после установки**. Содержит основную логику настройки.
*   `bootstrap.sh`: Скрипт-загрузчик. Его задача — скачать репозиторий и запустить `opkg/postinst`, передав ему все аргументы.

### Тестирование

Для разработки и тестирования используется Docker-окружение на базе **Alpine Linux**.

1.  **Установите Docker Desktop**.
2.  **Создайте и настройте файл `docker/.env`**:
    *   Скопируйте `docker/.env.example` в новый файл `docker/.env`.
    *   Заполните **все** поля: `BOT_TOKEN`, `USER_ID`, `API_ID`, `API_HASH`.
3.  **Запустите тесты:**
    *   `pytest -s`
    *   При первом запуске может потребоваться получить сессию `telethon`.
4.  **Принудительная пересборка образа (если меняли `Dockerfile`):**
    ```sh
    # В директории docker/
    docker-compose build --no-cache
    ```
