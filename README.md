# KDW Bot - Управление роутером Keenetic через Telegram

![Status](https://img.shields.io/badge/status-beta-orange.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)

Телеграм-бот для простого и удобного управления службами, прокси-конфигурациями и маршрутизацией трафика на роутерах Keenetic с установленной системой Entware.

## ⚠️ Статус проекта: Бета

Проект находится в стадии активной разработки. Используйте его с осторожностью. Перед установкой рекомендуется сделать резервную копию настроек роутера.

## 🚀 Основные возможности

*   **Простая установка:** Установка и настройка бота одной командой через SSH.
*   **Полный цикл управления:** Установка, обновление и полное удаление системы через единый скрипт.
*   **Чистое удаление:** Использование манифеста установки для 100% чистого удаления всех компонентов.
*   **Управление "по кнопке":** Настройка и управление всей системой через интуитивно понятное меню в Telegram.
*   **Гибкая маршрутизация:** Управление `iptables` и `ipset` через специальное меню, с автоматическим сохранением состояния между перезагрузками.

---

## 🤖 Меню и функции бота

После запуска бота командой `/start` вам будет доступно главное меню.

### 🌍 Система обхода

Этот раздел предназначен для управления ключевыми компонентами системы.

*   **Ключи:** Позволяет добавлять и управлять ключами для прокси-сервисов.
    *   ✅ **Shadowsocks, Trojan, Vmess:** Полная поддержка (добавление, удаление, активация, тест).
*   **Списки:** Управление маршрутизацией доменов. Система использует **раздельные списки** для каждого типа прокси.
    *   **Логика работы:** Вы выбираете тип прокси (например, `Trojan`) и добавляете в его список домены. Трафик к этим доменам будет направлен через соответствующий прокси.
    *   **Интеллектуальное добавление:** Если вы пытаетесь добавить домен, который уже находится в другом списке, бот предложит **автоматически переместить** его.
    *   **Доступные списки:** `shadowsocks`, `trojan`, `vmess`.

### ⚙️ Настройки

Этот раздел разделен на три логических блока для удобства управления.

#### 🖥️ Управление системой
*   **📊 Статус служб:** Показывает, активны или неактивны ключевые службы.
*   **⚙️ Перезагрузить службы:** Выполняет перезапуск всех служб обхода.
*   **🤖 Перезагрузить бота:** Выполняет "горячую" перезагрузку самого бота.
*   **🔄 Обновить:** Запускает процедуру обновления бота до последней версии.
*   **🗑️ Удалить:** Запускает процедуру полного удаления бота и его компонентов с роутера.

#### 🔧 Настройки бота
*   **📝 Уровень логов:** Позволяет динамически изменять детализацию логов бота.
*   **Пинг в списке:** Включает или отключает автоматическую проверку пинга при отображении списка ключей.
*   **Прокси для всего трафика:** Позволяет выбрать, какой тип прокси (`Trojan`, `Vmess` или `Shadowsocks`) будет использоваться, когда вы включаете режим "Применить правила для всего трафика".

#### 🔥 Правила Firewall
Этот раздел позволяет безопасно управлять правилами `iptables`. Бот автоматически определяет текущее состояние правил и помечает его галочкой ✅.

*   **Применить правила для списков:** "Умный" режим. Трафик направляется в прокси **только для доменов из ваших списков**. Весь остальной трафик идет напрямую.
*   **Применить правила для всего трафика:** Режим "полного туннелирования". **Весь трафик** с роутера направляется через один активный прокси, тип которого вы задаете в "Настройках бота".
*   **Сбросить все правила:** "Безопасный" режим. Полностью отключает проксирование на уровне `iptables`, удаляя все правила и `ipset`-списки, созданные ботом.

---

## 🛠️ Установка и управление

### Шаг 1: Подготовка роутера

1.  **Установите Entware:** Если у вас не установлен Entware, воспользуйтесь [официальной инструкцией Keenetic](https://help.keenetic.com/hc/ru/articles/360021214160-Установка-системы-пакетов-репозитория-Entware-на-USB-накопитель).
2.  **Подключитесь к роутеру по SSH.**

### Шаг 2: Установка и управление ботом

Все действия выполняются через единый скрипт `bootstrap.sh`. Он автоматически установит все необходимые зависимости (`curl`, `git`, `python3-pip` и т.д.).

*   **Установка:**
    Запускает полную установку с нуля. Скрипт сам задаст вопросы для первоначальной настройки.
    ```sh
    curl -H "Cache-Control: no-cache" -sL -o /tmp/bootstrap.sh https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh && sh /tmp/bootstrap.sh --install
    ```

*   **Обновление (рекомендуется):**
    Запускает "мягкое" обновление. Бот остановится, обновит свой код из репозитория (`git pull`), обновит Python-зависимости и снова запустится. Ваша конфигурация и системные пакеты (Entware) не будут затронуты.
    ```sh
    curl -H "Cache-Control: no-cache" -sL -o /tmp/bootstrap.sh https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh && sh /tmp/bootstrap.sh --update
    ```
    Для автоматического обновления без подтверждения добавьте флаг `-y`:
    ```sh
    sh /tmp/bootstrap.sh --update -y
    ```

*   **Полная переустановка (если "мягкое" обновление не помогло):**
    Если "мягкое" обновление не сработало или возникли проблемы, вы можете выполнить полную переустановку. Это действие полностью удалит старую версию бота, сохранит ваш конфиг и установит новую версию с нуля.
    ```sh
    # Сначала сохраните ваш kdw.cfg, если он был изменен вручную
    # cp /opt/etc/kdw/kdw.cfg /tmp/kdw.cfg.backup

    # Затем выполните полную переустановку
    curl -H "Cache-Control: no-cache" -sL -o /tmp/bootstrap.sh https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh && sh /tmp/bootstrap.sh --uninstall -y && sh /tmp/bootstrap.sh --install -y --token=<ВАШ_ТОКЕН> --user-id=<ВАШ_ID>
    ```
    Замените `<ВАШ_ТОКЕН>` и `<ВАШ_ID>` на актуальные значения.

*   **Удаление:**
    Полностью удаляет все, что связано с работой проекта, на основе манифеста установки.
    ```sh
    curl -H "Cache-Control: no-cache" -sL -o /tmp/bootstrap.sh https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh && sh /tmp/bootstrap.sh --uninstall
    ```

### Шаг 3: Настройка маршрутизации (iptables)

**Никаких ручных действий не требуется!**

Бот полностью берет на себя управление правилами `iptables` через меню **Настройки -> Правила Firewall**.

Выбранный вами режим будет **автоматически восстанавливаться** после перезагрузки роутера благодаря скриптам, которые бот создает в `/opt/etc/ndm/fs.d/`.

---

## ⚙️ Техническая архитектура

*   **Директория проекта:** `/opt/etc/kdw`
*   **Конфигурация бота:** `/opt/etc/kdw/kdw.cfg`
*   **Списки доменов:** `/opt/etc/kdw/lists/` (например, `trojan.list`)
*   **Скрипты управления Firewall:** `/opt/etc/kdw/scripts/`
*   **Файл состояния Firewall:** `/opt/etc/kdw/firewall_mode.state` (сохраняет ваш выбор для перезагрузки)
*   **Скрипт автозапуска Firewall:** `/opt/etc/ndm/fs.d/100-kdw-firewall.sh`
*   **Имена `ipset`:** `kdw_trojan`, `kdw_shadowsocks` и т.д.