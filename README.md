# KDW Bot - Управление роутером Keenetic через Telegram

![Status](https://img.shields.io/badge/status-beta-orange.svg)
![Tests](https://img.shields.io/badge/tests-passing-brightgreen.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)

Телеграм-бот для простого и удобного управления службами, прокси и списками обхода блокировок на роутерах Keenetic с установленной системой Entware.

## ⚠️ Статус проекта: Бета

Проект находится в стадии активной разработки. Используйте его с осторожностью. Перед установкой рекомендуется сделать резервную копию настроек роутера.

## 🚀 Основные возможности

*   **Простая установка:** Установка и настройка бота одной командой через SSH.
*   **Полный цикл управления:** Установка, обновление и полное удаление системы через единый скрипт.
*   **Чистое удаление:** Использование манифеста установки для 100% чистого удаления всех компонентов.
*   **Управление "по кнопке":** Настройка и управление всей системой обхода блокировок через интуитивно понятное меню в Telegram.

---

## 🤖 Меню и функции бота

После запуска бота командой `/start` вам будет доступно главное меню.

### 🌍 Система обхода

Этот раздел предназначен для управления ключевыми компонентами системы обхода блокировок.

*   **Ключи:** Позволяет добавлять и управлять ключами для прокси-сервисов.
    *   ✅ **Shadowsocks:** Полная поддержка (добавление, удаление, активация, тест).
    *   ✅ **Trojan:** Полная поддержка (добавление, удаление, активация, тест).
    *   🟡 **Vmess:** Функционал в разработке.
*   **Списки:** Управление списками доменов для обхода. Вы можете:
    *   Просматривать содержимое списков.
    *   Добавлять новые домены.
    *   Удалять существующие домены.

### 🌐 Роутер

*   🟡 **В разработке:** Этот раздел предназначен для будущего функционала по управлению настройками самого роутера.

### ⚙️ Настройки

Этот раздел содержит инструменты для управления самим ботом и службами.

*   **Статус служб:** Показывает, активны или неактивны ключевые службы (`Shadowsocks`, `Trojan`, `Vmess`, `Tor`).
*   **Уровень логов:** Позволяет динамически изменять детализацию логов бота. Доступные уровни: `INFO`, `WARNING`, `ERROR`, `DEBUG`. Выбор сохраняется между перезапусками.
*   **Перезагрузить службы:** Выполняет перезапуск всех служб обхода.
*   **Перезагрузить бота:** Выполняет "горячую" перезагрузку самого бота для применения изменений в коде.
*   **Обновить:** Запускает процедуру обновления бота до последней версии.
*   **Удалить:** Запускает процедуру полного удаления бота и его компонентов с роутера.

---

## 🚦 Тестирование

Для каждого ключа доступен функционал для проверки качества соединения.

### Быстрый тест (Пинг)

При отображении списка ключей для каждого из них автоматически выполняется быстрый тест (`ping`) для оценки доступности сервера и задержки канала.

*   **`Пинг: 45 мс`**: Сервер доступен, среднее время отклика канала — 45 миллисекунд.
*   **`Пинг: ❌`**: Сервер не отвечает на ICMP-запросы (возможно, недоступен или блокирует `ping`).
*   **`Пинг: ⚠️`**: Не удалось распознать ответ от утилиты `ping`.

### Полный тест

Для каждого ключа доступна кнопка "🚦 Тест", которая запускает более глубокую проверку.

#### Для Shadowsocks

Тест включает в себя измерение двух ключевых метрик:
1.  **Пинг**: Среднее время отклика до сервера (прямой канал).
2.  **Прокси**:
    *   **Задержка**: "Пинг через прокси". Время от запроса до получения первого байта ответа.
    *   **Скорость**: Средняя скорость скачивания тестового файла.

#### Для Trojan

Из-за технических особенностей, полный тест для Trojan требует **временной остановки сервиса** (бот запросит подтверждение) и измеряет те же метрики, что и для Shadowsocks.

---

## 🛠️ Установка и управление

### Шаг 1: Подготовка роутера

1.  **Установите Entware:** Если у вас не установлен Entware, воспользуйтесь [официальной инструкцией Keenetic](https://help.keenetic.com/hc/ru/articles/360021214160-Установка-системы-пакетов-репозитория-Entware-на-USB-накопитель).
2.  **Подключитесь к роутеру по SSH.**
3.  **Установите `curl`:** Выполните команду, чтобы установить `curl` с поддержкой HTTPS. Это **обязательный** шаг.
    ```sh
    opkg update && opkg install curl
    ```

### Шаг 2: Установка и управление

Все действия выполняются через единый скрипт `bootstrap.sh`.

*   **Установка:**
    Запускает полную установку с нуля. Скрипт сам установит все зависимости и задаст необходимые вопросы.
    ```sh
    curl -H "Cache-Control: no-cache" -sL -o /tmp/bootstrap.sh https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh && sh /tmp/bootstrap.sh --install
    ```
    *(Флаг `--install` можно опустить, так как это действие по умолчанию)*

*   **Обновление:**
    Скачивает последнюю версию, сохраняет ваш конфиг, полностью удаляет старую версию и устанавливает новую, автоматически восстанавливая конфиг.
    ```sh
    curl -H "Cache-Control: no-cache" -sL -o /tmp/bootstrap.sh https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh && sh /tmp/bootstrap.sh --update
    ```

*   **Удаление:**
    Полностью удаляет все, что связано с работой проекта, на основе манифеста установки: останавливает службу, удаляет все файлы и директории, а также все системные пакеты, установленные вместе с ботом.
    ```sh
    curl -H "Cache-Control: no-cache" -sL -o /tmp/bootstrap.sh https://raw.githubusercontent.com/xxsok/PycharmProjects/KDW/bootstrap.sh && sh /tmp/bootstrap.sh --uninstall
    ```

---

## 🐳 Для разработчиков

Проект использует "пакетную" архитектуру и тестируется в Docker-окружении на базе **Alpine Linux**. Подробности см. в `tests/conftest.py` и `docker/Dockerfile`.
