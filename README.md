# KDW Bot - Управление роутером Keenetic через Telegram

Телеграм-бот для простого и удобного управления службами, прокси и списками обхода блокировок на роутерах Keenetic с установленной системой Entware.

## 🚀 Основные возможности
*   **Установка одной командой:** Полная установка и настройка бота на роутер.
*   **Управление "по кнопке":** Установка и настройка всей системы обхода блокировок через меню в Telegram.
*   **Управление конфигурациями, списками и службами.**

## ⚙️ Установка для пользователей

1.  Убедитесь, что на роутере Keenetic установлен компонент системы **"Entware"**.
2.  Зайдите в веб-интерфейс роутера, перейдите в "Диагностика" -> "Командная строка".
3.  Вставьте и выполните команду:
    ```sh
    curl -sL https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh | sh
    ```
4.  Следуйте инструкциям в консоли (скрипт запросит токен бота и ваш ID).

---

## 🐳 Для разработчиков: Запуск в Docker

### Настройка

1.  **Установите Docker Desktop**.
2.  **Перейдите в директорию `docker`**: `cd docker`
3.  **Создайте и настройте файл `.env`**:
    *   Скопируйте `docker/.env.example` в новый файл `docker/.env`.
    *   Заполните `BOT_TOKEN` и `USER_ID`.
    *   Заполните `API_ID` и `API_HASH` (получите их на [my.telegram.org](https://my.telegram.org)).

### Запуск тестов и получение сессии

Для E2E-тестов используется ваш личный аккаунт, чтобы отправлять команды боту. При первом запуске тестов нужно пройти авторизацию.

1.  **Установите зависимости:**
    ```sh
    pip install -r requirements.txt
    ```
2.  **Запустите тесты в интерактивном режиме:**
    ```sh
    pytest -s
    ```
3.  **Пройдите авторизацию:** `telethon` запросит в консоли ваш номер телефона, код из Telegram и, возможно, пароль.
    *   **Формат телефона:** Вводите номер в международном формате, начиная со знака `+`. Например: `+79991234567`.
4.  **Сохраните сессию:** После успешного входа в консоли появится блок с сессионной строкой. Скопируйте эту строку и вставьте в `docker/.env` в поле `USER_SESSION`.

После сохранения `USER_SESSION` все последующие запуски тестов будут проходить автоматически, без запроса телефона и кода.

### Запуск Docker Compose

*   **Через терминал:**
    ```sh
    # Находясь в папке docker/
    docker-compose up --build
    ```
*   **Через PyCharm:** Откройте файл `docker/docker-compose.yml` и нажмите на зеленые стрелочки рядом с `services:`.
