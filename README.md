# KDW Bot - Управление роутером Keenetic через Telegram

![Status](https://img.shields.io/badge/status-beta-orange.svg)
![Tests](https://img.shields.io/badge/tests-passing-brightgreen.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)

Телеграм-бот для простого и удобного управления службами, прокси и списками обхода блокировок на роутерах Keenetic с установленной системой Entware.

## ⚠️ Статус проекта: Бета

Проект находится в стадии активной разработки. Используйте его с осторожностью. Перед установкой рекомендуется сделать резервную копию настроек роутера.

## 🚀 Основные возможности

*   **Простая установка:** Установка и интерактивная настройка бота через SSH.
*   **Полный цикл управления:** Установка, переустановка и полное удаление системы.
*   **Управление "по кнопке":** Настройка и управление всей системой обхода блокировок через интуитивно понятное меню в Telegram.
*   **Безопасное удаление:** Полное удаление всех компонентов системы с подтверждением в Telegram.

## ⚙️ Управление установкой

### Шаг 1: Подготовка роутера

1.  **Установите Entware:** Если у вас не установлен Entware, воспользуйтесь [официальной инструкцией Keenetic](https://help.keenetic.com/hc/ru/articles/360021214160-Установка-системы-пакетов-репозитория-Entware-на-USB-накопитель).
2.  **Подключитесь к роутеру по SSH.**
3.  **Установите `curl` и `jq`:** Выполните следующие команды, чтобы обновить списки пакетов и установить необходимые утилиты. Это **обязательный** шаг.
    ```sh
    opkg update
    opkg install curl jq
    ```

### Шаг 2: Установка / Управление KDW Bot

Все действия выполняются через единый скрипт `bootstrap.sh`, который нужно сначала скачать, а потом запустить.

*   **Установка (интерактивно):**
    ```sh
    curl -sL -o /tmp/bootstrap.sh https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh && sh /tmp/bootstrap.sh
    ```
    Скрипт сам задаст все необходимые вопросы (токен бота, ваш ID).

*   **Установка (автоматически):**
    ```sh
    curl -sL -o /tmp/bootstrap.sh https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh && sh /tmp/bootstrap.sh -- --token YOUR_TOKEN --user-id YOUR_ID
    ```

*   **Переустановка:**
    ```sh
    curl -sL -o /tmp/bootstrap.sh https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh && Sh /tmp/bootstrap.sh -- --reinstall
    ```

*   **Удаление:**
    ```sh
    curl -sL -o /tmp/bootstrap.sh https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh && sh /tmp/bootstrap.sh -- --uninstall
    ```

---

## 🐳 Для разработчиков

Проект использует "пакетную" архитектуру и тестируется в Docker-окружении на базе **Alpine Linux**. Подробности см. в `tests/conftest.py` и `docker/Dockerfile`.
