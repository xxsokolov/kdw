# KDW Bot - Управление роутером Keenetic через Telegram

![Status](https://img.shields.io/badge/status-beta-orange.svg)
![Tests](https://img.shields.io/badge/tests-passing-brightgreen.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)

Телеграм-бот для простого и удобного управления службами, прокси-конфигурациями и маршрутизацией трафика на роутерах Keenetic с установленной системой Entware.

## ⚠️ Статус проекта: Бета

Проект находится в стадии активной разработки. Используйте его с осторожностью. Перед установкой рекомендуется сделать резервную копию настроек роутера.

## 🚀 Основные возможности

*   **Простая установка:** Установка и настройка бота одной командой через SSH.
*   **Полный цикл управления:** Установка, обновление и полное удаление системы через единый скрипт.
*   **Чистое удаление:** Использование манифеста установки для 100% чистого удаления всех компонентов.
*   **Управление "по кнопке":** Настройка и управление всей системой через интуитивно понятное меню в Telegram.
*   **Гибкая маршрутизация:** Управление направлением трафика для конкретных доменов через разные прокси-каналы.

---

## 🤖 Меню и функции бота

После запуска бота командой `/start` вам будет доступно главное меню.

### 🌍 Система обхода

Этот раздел предназначен для управления ключевыми компонентами системы.

*   **Ключи:** Позволяет добавлять и управлять ключами для прокси-сервисов.
    *   ✅ **Shadowsocks:** Полная поддержка (добавление, удаление, активация, тест).
    *   ✅ **Trojan:** Полная поддержка (добавление, удаление, активация, тест).
    *   🟡 **Vmess:** Частичная поддержка (распознавание типа ключа).
*   **Списки:** Управление маршрутизацией доменов. Система использует **раздельные списки** для каждого типа прокси.
    *   **Логика работы:** Вы выбираете тип прокси (например, `Trojan`) и добавляете в его список домены. Трафик к этим доменам будет направлен через соответствующий прокси.
    *   **Интеллектуальное добавление:** Если вы пытаетесь добавить домен, который уже находится в другом списке, бот предложит **автоматически переместить** его. Это гарантирует, что каждый домен в один момент времени может быть направлен только в один канал.
    *   **Доступные списки:** `Shadowsocks`, `Trojan`, `Vmess`, `Direct` (для прямого соединения в обход прокси).

### 🌐 Роутер

*   🟡 **В разработке:** Этот раздел предназначен для будущего функционала по управлению настройками самого роутера.

### ⚙️ Настройки

Этот раздел содержит инструменты для управления самим ботом и службами.

*   **Статус служб:** Показывает, активны или неактивны ключевые службы (`Shadowsocks`, `Trojan`, `Vmess`, `Tor`).
*   **Уровень логов:** Позволяет динамически изменять детализацию логов бота.
*   **Пинг в списке:** Включает или отключает автоматическую проверку пинга при отображении списка ключей.
*   **Перезагрузить службы:** Выполняет перезапуск всех служб обхода.
*   **Перезагрузить бота:** Выполняет "горячую" перезагрузку самого бота.
*   **Обновить:** Запускает процедуру обновления бота до последней версии.
*   **Удалить:** Запускает процедуру полного удаления бота и его компонентов с роутера.

---

## 🛠️ Установка и управление

Процесс установки состоит из трех шагов.

### Шаг 1: Подготовка роутера

1.  **Установите Entware:** Если у вас не установлен Entware, воспользуйтесь [официальной инструкцией Keenetic](https://help.keenetic.com/hc/ru/articles/360021214160-Установка-системы-пакетов-репозитория-Entware-на-USB-накопитель).
2.  **Подключитесь к роутеру по SSH.**
3.  **Установите необходимые пакеты:** Выполните команду, чтобы установить `curl`, `git` и `pip`. Это **обязательный** шаг.
    ```sh
    opkg update
    opkg install curl git python3-pip
    ```

### Шаг 2: Установка бота

Все действия выполняются через единый скрипт `bootstrap.sh`.

*   **Установка:**
    Запускает полную установку с нуля. Скрипт сам установит все зависимости, создаст необходимые директории и скрипты, а также задаст вопросы для первоначальной настройки.
    ```sh
    curl -H "Cache-Control: no-cache" -sL -o /tmp/bootstrap.sh https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh && sh /tmp/bootstrap.sh --install
    ```
    *(Флаг `--install` можно опустить, так как это действие по умолчанию)*

*   **Обновление:**
    Скачивает последнюю версию, сохраняет ваш конфиг, полностью удаляет старую версию и устанавливает новую, автоматически восстанавливая конфиг.
    ```sh
    curl -H "Cache-Control: no-cache" -sL -o /tmp/bootstrap.sh https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh && sh /tmp/bootstrap.sh --update
    ```

*   **Удаление:**
    Полностью удаляет все, что связано с работой проекта, на основе манифеста установки.
    ```sh
    curl -H "Cache-Control: no-cache" -sL -o /tmp/bootstrap.sh https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh && sh /tmp/bootstrap.sh --uninstall
    ```

### Шаг 3: Настройка маршрутизации (iptables)

После установки бота необходимо **один раз** настроить правила файрвола, чтобы роутер начал использовать списки, управляемые ботом.

1.  **Подключитесь к роутеру по SSH.**
2.  **Создайте собственную цепочку правил** для инкапсуляции всей логики KDW. Это лучшая практика, которая предотвращает конфликты с другими правилами.
    ```sh
    iptables -t mangle -N KDW_RULES
    ```
3.  **Направьте трафик в вашу цепочку.** Это правило нужно добавить в самое начало цепочки `PREROUTING`.
    ```sh
    iptables -t mangle -I PREROUTING 1 -j KDW_RULES
    ```
4.  **Добавьте правила для каждого списка.** Внутри вашей цепочки `KDW_RULES` добавьте правила, которые будут маркировать трафик для доменов из ваших списков. **Это только пример!** Вам нужно адаптировать его под вашу систему маркировки и маршрутизации.
    ```sh
    # Пример: маркируем трафик для Trojan меткой "10"
    iptables -t mangle -A KDW_RULES -m set --match-set kdw_trojan dst -j MARK --set-mark 10

    # Пример: маркируем трафик для Shadowsocks меткой "20"
    iptables -t mangle -A KDW_RULES -m set --match-set kdw_shadowsocks dst -j MARK --set-mark 20
    
    # Пример: для списка "direct" просто возвращаем трафик в основную цепочку, прекращая его обработку здесь
    iptables -t mangle -A KDW_RULES -m set --match-set kdw_direct dst -j RETURN
    ```
5.  **Сохраните правила.** На роутерах Keenetic для сохранения правил `iptables` между перезагрузками обычно используется файл `/opt/etc/ndm/netfilter.d/100-kdw.sh` или аналогичный. Создайте его и поместите туда ваши команды из пунктов 2-4.
    ```sh
    # Пример файла /opt/etc/ndm/netfilter.d/100-kdw.sh
    #!/bin/sh
    iptables -t mangle -N KDW_RULES
    iptables -t mangle -I PREROUTING 1 -j KDW_RULES
    iptables -t mangle -A KDW_RULES -m set --match-set kdw_trojan dst -j MARK --set-mark 10
    iptables -t mangle -A KDW_RULES -m set --match-set kdw_shadowsocks dst -j MARK --set-mark 20
    iptables -t mangle -A KDW_RULES -m set --match-set kdw_direct dst -j RETURN
    ```
    Не забудьте сделать его исполняемым: `chmod +x /opt/etc/ndm/netfilter.d/100-kdw.sh`.

---

## ⚙️ Техническая архитектура

*   **Директория проекта:** `/opt/etc/kdw`
*   **Списки доменов:** `/opt/etc/kdw/lists/` (например, `trojan.list`)
*   **Скрипт применения правил:** `/opt/etc/kdw/scripts/apply_lists.sh`
*   **Имена `ipset`:** `kdw_trojan`, `kdw_shadowsocks` и т.д.
*   **Цепочка `iptables`:** `KDW_RULES` (рекомендуется)
