# KDW Bot - Управление роутером Keenetic через Telegram

![Status](https://img.shields.io/badge/status-beta-orange.svg)
![Tests](https://img.shields.io/badge/tests-passing-brightgreen.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)

Телеграм-бот для простого и удобного управления службами, прокси и списками обхода блокировок на роутерах Keenetic с установленной системой Entware.

## ⚠️ Статус проекта: Бета

Проект находится в стадии активной разработки. Используйте его с осторожностью. Перед установкой рекомендуется сделать резервную копию настроек роутера.

## 🚀 Основные возможности

*   **Установка одной командой:** Полная установка и настройка бота на роутер.
*   **Интерактивная настройка:** Бот сам запрашивает токен и ID пользователя в процессе установки.
*   **Управление "по кнопке":** Установка, настройка и управление всей системой обхода блокировок через интуитивно понятное меню в Telegram.
*   **Управление конфигурациями:** Удобное добавление и редактирование ключей для Shadowsocks.
*   **Управление списками обхода:** Простое добавление и удаление доменов в списки для проксирования.
*   **Безопасное удаление:** Полное удаление всех компонентов системы с подтверждением.

## ⚙️ Установка для пользователей

1.  Убедитесь, что на роутере Keenetic установлен компонент системы **"Entware"**.
2.  Зайдите в веб-интерфейс роутера, перейдите в "Диагностика" -> "Командная строка".
3.  Вставьте и выполните команду:
    ```sh
    curl -sL https://raw.githubusercontent.com/xxsokolov/KDW/main/bootstrap.sh | sh
    ```
4.  Следуйте инструкциям в консоли. Скрипт сам запросит у вас токен бота и ваш Telegram User ID, проверит их и выполнит всю остальную установку автоматически.

После завершения работы скрипта бот будет установлен, настроен и запущен.

---

## 🐳 Для разработчиков

### Архитектура проекта

Проект использует "пакетную" архитектуру, имитирующую сборку пакетов OPKG.

*   `opkg/`: Директория содержит скрипты, которые выполняются на разных этапах "жизни" пакета.
    *   `control`: Метаданные пакета.
    *   `postinst`: Скрипт, выполняемый **после установки**. Содержит основную логику настройки (запрос токенов, создание конфигов).
    *   `prerm`: Скрипт, выполняемый **до удаления**. Останавливает службы.
    *   `postrm`: Скрипт, выполняемый **после удаления**. "Подчищает" все созданные файлы.
*   `bootstrap.sh`: Скрипт-загрузчик. Его единственная задача — скачать репозиторий и запустить `opkg/postinst`, имитируя установку пакета.

### Тестирование

Для разработки и тестирования используется Docker-окружение на базе **Alpine Linux**.

*   **Юнит-тесты:** Проверяют изолированную логику `core` модулей.
*   **E2E-тесты:** Запускают полный сценарий взаимодействия с ботом в Docker-контейнере.

**Важно:** Тестовое окружение **не выполняет** "боевые" скрипты (`install.sh`, `uninstall.sh`). Вместо этого, E2E-тесты **мокируют** результат их работы (например, создают файл-маркер `S99unblock`), чтобы проверить, как бот реагирует на изменение состояния системы.

### Запуск тестов

1.  **Установите Docker Desktop**.
2.  **Создайте и настройте файл `docker/.env`**:
    *   Скопируйте `docker/.env.example` в новый файл `docker/.env`.
    *   Заполните **все** поля: `BOT_TOKEN`, `USER_ID`, `API_ID`, `API_HASH`.
3.  **Получите сессию пользователя (один раз):**
    *   Запустите тесты: `pytest -s`
    *   `telethon` запросит в консоли ваш номер телефона (в международном формате, например, `+7...`), код из Telegram и, возможно, пароль.
    *   После входа в консоли появится сессионная строка. Скопируйте ее и вставьте в `docker/.env` в поле `USER_SESSION`.
4.  **Запустите тесты снова:**
    *   `pytest -s`
    *   При последующих запусках тесты будут проходить автоматически, без запроса авторизации.
5.  **Принудительная пересборка образа (если меняли `Dockerfile`):**
    ```sh
    # В директории docker/
    docker-compose build --no-cache
    ```
